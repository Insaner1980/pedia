<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Pedia.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="800"
    d:DesignWidth="1200"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource PediaApplicationBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Padding="22,14" Background="{ThemeResource PediaPrimaryPaneBrush}" BorderBrush="{ThemeResource PediaSubtleDividerBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button Command="{x:Bind ViewModel.BackCommand}" Style="{StaticResource PediaToolbarButtonStyle}">
                    <Button.KeyboardAccelerators>
                        <KeyboardAccelerator Key="Left" Modifiers="Menu" />
                    </Button.KeyboardAccelerators>
                    <StackPanel Orientation="Horizontal" Spacing="7">
                        <FontIcon Glyph="&#xE72B;" />
                        <TextBlock x:Uid="BackButton" />
                    </StackPanel>
                </Button>
                <TextBlock x:Uid="SettingsHeader" VerticalAlignment="Center" FontSize="22" FontWeight="SemiBold" />
            </StackPanel>
            <ProgressRing HorizontalAlignment="Right" VerticalAlignment="Center" Width="24" Height="24" IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}" />
        </Grid>

        <ScrollViewer Grid.Row="1" IsEnabled="{x:Bind ViewModel.CanInteract, Mode=OneWay}" VerticalScrollBarVisibility="Auto">
            <StackPanel Width="760" Margin="36,26,36,64" HorizontalAlignment="Center" Spacing="28">
                <StackPanel Spacing="14">
                    <TextBlock x:Uid="GeneralSettingsHeader" FontSize="20" FontWeight="SemiBold" />
                    <Grid ColumnSpacing="14" RowSpacing="12">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                        <TextBox x:Uid="DefaultLanguageField" Text="{x:Bind ViewModel.DefaultLanguageCode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MaxLength="35" />
                        <ComboBox x:Uid="DefaultStatusField" Grid.Column="1" ItemsSource="{x:Bind ViewModel.Statuses}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{x:Bind ViewModel.DefaultArticleStatus, Mode=TwoWay}" HorizontalAlignment="Stretch" />
                        <CheckBox x:Uid="RestoreSelectionField" Grid.Row="1" Grid.ColumnSpan="2" IsChecked="{x:Bind ViewModel.RestoreLastArticle, Mode=TwoWay}" />
                        <CheckBox x:Uid="ConfirmTrashField" Grid.Row="2" Grid.ColumnSpan="2" IsChecked="{x:Bind ViewModel.ConfirmBeforeTrash, Mode=TwoWay}" />
                        <CheckBox x:Uid="DefaultIncludeSubtopicsField" Grid.Row="3" IsChecked="{x:Bind ViewModel.IncludeSubtopicsByDefault, Mode=TwoWay}" />
                        <ComboBox x:Uid="PageSizeField" Grid.Row="3" Grid.Column="1" ItemsSource="{x:Bind ViewModel.PageSizes}" SelectedItem="{x:Bind ViewModel.PageSize, Mode=TwoWay}" HorizontalAlignment="Stretch" />
                    </Grid>
                </StackPanel>

                <Border Height="1" Background="{ThemeResource PediaSubtleDividerBrush}" />
                <StackPanel Spacing="14">
                    <TextBlock x:Uid="ReadingSettingsHeader" FontSize="20" FontWeight="SemiBold" />
                    <Grid ColumnSpacing="14" RowSpacing="12">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                        <NumberBox x:Uid="BodyFontSizeField" Value="{x:Bind ViewModel.ArticleBodyFontSize, Mode=TwoWay}" Minimum="13" Maximum="24" SpinButtonPlacementMode="Compact" />
                        <NumberBox x:Uid="LineSpacingField" Grid.Column="1" Value="{x:Bind ViewModel.ArticleLineSpacing, Mode=TwoWay}" Minimum="19" Maximum="38" SpinButtonPlacementMode="Compact" />
                        <NumberBox x:Uid="MaximumReadingWidthField" Grid.Row="1" Value="{x:Bind ViewModel.MaximumReadingWidth, Mode=TwoWay}" Minimum="600" Maximum="1100" SpinButtonPlacementMode="Compact" />
                        <CheckBox x:Uid="RememberScrollField" Grid.Row="1" Grid.Column="1" VerticalAlignment="Bottom" IsChecked="{x:Bind ViewModel.RememberScrollPositions, Mode=TwoWay}" />
                    </Grid>
                </StackPanel>

                <Border Height="1" Background="{ThemeResource PediaSubtleDividerBrush}" />
                <StackPanel Spacing="12">
                    <TextBlock x:Uid="AppearanceSettingsHeader" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock x:Uid="ThemeInformation" Style="{StaticResource PediaMetadataTextStyle}" TextWrapping="Wrap" />
                    <ToggleSwitch x:Uid="DensitySwitch" IsOn="{x:Bind ViewModel.CompactDensity, Mode=TwoWay}" />
                </StackPanel>

                <Border Height="1" Background="{ThemeResource PediaSubtleDividerBrush}" />
                <StackPanel Spacing="14">
                    <TextBlock x:Uid="DataSettingsHeader" FontSize="20" FontWeight="SemiBold" />
                    <Grid ColumnSpacing="18" RowSpacing="10">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="170" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock x:Uid="DatabasePathLabel" Style="{StaticResource PediaMutedTextStyle}" />
                        <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.DatabasePath, Mode=OneWay}" IsTextSelectionEnabled="True" TextWrapping="Wrap" />
                        <TextBlock x:Uid="DatabaseSizeLabel" Grid.Row="1" Style="{StaticResource PediaMutedTextStyle}" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{x:Bind ViewModel.DatabaseSize, Mode=OneWay}" />
                        <TextBlock x:Uid="SchemaVersionLabel" Grid.Row="2" Style="{StaticResource PediaMutedTextStyle}" />
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{x:Bind ViewModel.SchemaVersion, Mode=OneWay}" />
                        <TextBlock x:Uid="ArticleCountLabel" Grid.Row="3" Style="{StaticResource PediaMutedTextStyle}" />
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{x:Bind ViewModel.ArticleCount, Mode=OneWay}" />
                        <TextBlock x:Uid="TopicCountLabel" Grid.Row="4" Style="{StaticResource PediaMutedTextStyle}" />
                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{x:Bind ViewModel.TopicCount, Mode=OneWay}" />
                        <TextBlock x:Uid="SourceCountLabel" Grid.Row="5" Style="{StaticResource PediaMutedTextStyle}" />
                        <TextBlock Grid.Row="5" Grid.Column="1" Text="{x:Bind ViewModel.SourceCount, Mode=OneWay}" />
                        <TextBlock x:Uid="SearchIndexLabel" Grid.Row="6" Style="{StaticResource PediaMutedTextStyle}" />
                        <TextBlock Grid.Row="6" Grid.Column="1" Text="{x:Bind ViewModel.SearchIndexState, Mode=OneWay}" />
                    </Grid>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button x:Uid="OpenDataFolderButton" Command="{x:Bind ViewModel.OpenDataFolderCommand}" />
                        <Button x:Uid="BackupNowButton" Command="{x:Bind ViewModel.BackupNowCommand}" />
                        <Button x:Uid="RestoreBackupButton" Command="{x:Bind ViewModel.RestoreBackupCommand}" />
                        <Button x:Uid="RebuildIndexButton" Command="{x:Bind ViewModel.RebuildSearchIndexCommand}" />
                        <Button x:Uid="DeleteSampleContentButton" Command="{x:Bind ViewModel.DeleteSampleContentCommand}" />
                    </StackPanel>
                </StackPanel>

                <Border Height="1" Background="{ThemeResource PediaSubtleDividerBrush}" />
                <StackPanel Spacing="8">
                    <TextBlock x:Uid="AboutSettingsHeader" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock x:Uid="AboutTitle" FontSize="17" FontWeight="SemiBold" />
                    <TextBlock x:Uid="AboutDescription" Style="{StaticResource PediaMetadataTextStyle}" />
                    <TextBlock Text="{x:Bind ViewModel.VersionText, Mode=OneWay}" Style="{StaticResource PediaMutedTextStyle}" />
                    <TextBlock x:Uid="PrivacyText" TextWrapping="Wrap" Style="{StaticResource PediaMetadataTextStyle}" />
                    <TextBlock x:Uid="ThirdPartyComponentsText" TextWrapping="Wrap" Style="{StaticResource PediaMutedTextStyle}" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2" Padding="18,10" Background="{ThemeResource PediaPrimaryPaneBrush}" BorderBrush="{ThemeResource PediaSubtleDividerBrush}" BorderThickness="0,1,0,0">
            <Button x:Uid="SaveSettingsButton" HorizontalAlignment="Right" Command="{x:Bind ViewModel.SaveSettingsCommand}" />
        </Grid>
    </Grid>
</UserControl>
